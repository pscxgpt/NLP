# Python 3.10+
# Install PyTorch with the CUDA build that matches your driver first:
#   https://pytorch.org/get-started/locally/
torch>=2.4          # torch.amp.GradScaler(device=...) signature
transformers>=4.40
tokenizers>=0.19
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.13
